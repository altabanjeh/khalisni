# Mobile and RTL Acceptance

Existing visual acceptance covers 360, 390, 430, 768, desktop, Arabic RTL, and English LTR spot checks. The public shell uses language context to set document direction; cards and public navigation have RTL-aware behavior.

Status: PARTIAL because the test suite contains no committed device/RTL E2E implementation for an independent rerun.
