# KHALSNI — Defect Register

Gate 1 · 2026-09-05 · HEAD `574753e`

Severity: BLOCKER / CRITICAL / HIGH / MEDIUM / LOW. Priority: P0 / P1 / P2 / P3 (see `KHALSNI_REMEDIATION_PLAN.md` §1 for classification rules).
**No BLOCKER or CRITICAL defect was found by this review. No P0.**
Findings are from this session's own source review unless the "Source" note says otherwise. Confidence is stated where a defect could not be dynamically confirmed.

---

## INDEPENDENT FINAL ACCEPTANCE GATE (2026-09-05) — see `KHALSNI_FINAL_ACCEPTANCE.md`

Adversarial re-verification found **no new P0/P1/security/data-integrity/workflow defect**. Evidence: 283 backend tests OK (+26 new adversarial: negative-input, mass-assignment, IDOR, invalid-workflow, auth-edge, soft-delete-leakage, full-flow data-integrity — all pass), 20/20 journeys, 81 authz assertions, 73/73 route checks, 95 screenshots (0 overflow, 0 console errors, AR+EN, 5 viewports), axe 10/10 clean, `check --deploy` 0 warnings, `npm audit --omit=dev`=0, `pip-audit`=0, no secrets in bundle, live `/media/secure_orders|payments|..` → 404.

New finding:

| ID | Severity | Status | Detail |
|---|---|---|---|
| **F-1** | P2 | OPEN (non-blocking) | Admin management **tables** show untranslated English column headers ("Actions"/"Status"/"Slug"/…) and some cell values ("Active"/"Visible"/"Expected completion: N days"/English category names) on the Arabic-first UI. Page chrome, nav, buttons, empty states are localized. Consequence: internal-ops screens read inconsistently in Arabic; English is a valid fallback; all functionality works. Matches the pre-existing "untranslated dev text" note. **Not a release blocker.** Fix: wrap admin `DataTable` headers + status/delivery/label cell formatters in `t(...)` keys. |

Re-verified FIXED (no regression): D1, D2, D3, D4, D5, D7, D8, D-DEP1, D-A11Y-1, D-UX1, D-UX2, D-DB1, D-DB2, U14, U7(partial), D6(advance). C-VIS1 remains `BLOCKED_PRODUCT_DECISION`.

---

## GATE 2 (round 2) — Gate 1R backlog remediation (2026-09-05)

| ID | Status | Evidence |
|---|---|---|
| **D-DEP1** | **FIXED / VERIFIED** | `react-router-dom` 7.14.2→**7.18.3**, `axios` 1.9→**1.20.0** (pulls `form-data` 4.0.6). `npm audit --omit=dev` → **"found 0 vulnerabilities"** (was 4 high). FE vitest 44/44, lint clean, `npm run build` passes; runtime route matrix re-run (routing library changed). |
| **D-A11Y-1** | **FIXED / VERIFIED** | Design-system token `--kh-text-subtle` `#98a2b3`→**`#5b6470`** (2.44:1 → ≈5.5:1 on the muted surface). Single token; propagates to every `ServiceCard`/small-label use. axe re-run (route matrix) — see runtime result below. |
| **D-UX1** | **FIXED / VERIFIED** | `LanguageSwitcher` now rendered in `PublicLayout` — desktop actions cluster **and** mobile menu drawer. Public visitor can switch AR↔EN; `dir` flips. Uses the existing reusable component (rule 15). |
| **D-UX2** | **FIXED / VERIFIED** | Homepage "services by category" section: fixed-width scroll rail → **responsive `grid gap-4 sm:grid-cols-2 xl:grid-cols-3`**. Cards fill the row and wrap; no left-flush empty space; 0 overflow (route matrix). |
| **D-DB1** | **FIXED (non-breaking) / VERIFIED** | `core.models.SoftDeleteQuerySet` (`.alive()/.active()/.deleted()/.with_deleted()`) exposed via `objects = SoftDeleteQuerySet.as_manager()` on `SoftDeleteModel`. **Default `.all()` unchanged** (no filtering regression, no migration). Satisfies A2 §2 literally. `makemigrations --check` clean; targeted suite green. |
| **D6 / CE10** | **ADVANCED (non-destructive) / VERIFIED** | `ServiceRequiredDocument.ensure_document_definition()` auto-creates/links a canonical `RequiredDocumentDefinition` from free-text `document_type` on every save; data migration `services/0011` backfills all existing rows (idempotent, mirrors `normalize_required_documents`). Legacy columns kept + synced (rule 11). Full column drop + non-null FK still **DEFERRED** (needs a real staging-data audit). |
| **U7** | **PARTIAL / VERIFIED** | `.field` primitive → logical inline padding (`ps-4 pe-4`); the shared search-input pattern across 9 admin/staff screens → `start-3` icon + `ps-9`; `text-right`/`text-left` → `text-start` and `border-r`→`border-e` in shared components (`NotificationPanel`, `InlineHelp`, `HelpGuidePanel`, employee pages). Remaining physical props are explicit `isArabic?` conditionals, centered toasts, and the Arabic-first sidebar drawer — acceptable. lint clean; RTL/LTR re-checked at runtime. |
| **U1–U6, U8–U13** | **DEFERRED_WITH_REASON** | Subjective visual reconciliation (box-in-box density, radius-scale unification, gray-overuse, photographic-vs-plain heroes, repeated info) needs a design decision + visual iteration; prior UI-team passes already attempted; changing them blind risks regressing the prior 180-route browser audit. Requires: design review + `C-VIS1` resolution. |
| **C-VIS1** | **BLOCKED_PRODUCT_DECISION** | 2026-08-05 dark-navy homepage mockup vs the light-direction written chain (2026-07-27 spec + 2026-09-01 reports). Light is the defensible current authority and the live site conforms to it, but the reference folder is internally inconsistent. Consequence: cannot certify "matches the approved reference" unambiguously. Required input: product owner confirms light vs dark **in writing**. |
| **D-DB3** | **DEFERRED (P3)** | Postgres CI service job — infra task, no code impact. |
| **U15** | **DEFERRED (P3)** | `App.css` starter cruft / `vite.svg`+`react.svg` / template README — cosmetic. |

---

## GATE 1R — runtime verification & new findings (2026-09-05)

**Full backend suite after Gate 1R additions: 249 tests, all pass** (218 + 20 journey + 4 authz-matrix + 7 contract). `manage.py check` clean; `check --deploy` under prod config = 1 warning only (`SECRET_KEY` length — expected with a test key). Migration drift clean.

**Runtime re-verification of prior defects (live seeded stack + Playwright/system-Chrome):**

| ID | Runtime result |
|---|---|
| D1 | **CONFIRMED FIXED at runtime.** `GET /media/secure_orders/…`, `/media/payments/receipts/…`, `/media/../…` all → **404**; public `/media/services/images/…` → 404 (from static serve, i.e. guard passed). nginx also denies the sensitive prefixes. Severity stays **MEDIUM** (was: exploitable only with a leaked uuid path; now not reachable at all via `/media/`). |
| D2 | **CONFIRMED FIXED.** Live `pricing` payload gates fee parts to `null`; contract test proves `related_services[]` have no raw fee keys. |
| D3 | **CONFIRMED FIXED.** `DEBUG=False` + `SECURE_SSL_REDIRECT=True` with env unset (executed). `check --deploy` prod-config warnings all clear except key length. |
| D4 | **CONFIRMED FIXED.** Old access token → 401 (`token_revoked`) after logout-all; `token_version` claim present; 30-min lifetime; rotating refresh persisted by FE. |
| D10 | **CLOSED (harness now executes).** Committed Playwright harness (`frontend/e2e/`, `channel:'chrome'`) executed this gate: 73/73 route-runtime checks, 20/20 journeys (API), 78 authz assertions, axe on 10 routes, screenshots AR+EN. No longer "harness only". |
| A11Y-1 | **PARTIALLY CLOSED → reclassified as D-A11Y-1** with a concrete root cause (see new findings). axe now runs in CI. |

**New findings from Gate 1R runtime evidence:**

| ID | Severity | Priority | Area | Current behaviour | Expected | Root cause | Fix | Test |
|---|---|---|---|---|---|---|---|---|
| **D-A11Y-1** | MEDIUM | P2 | Accessibility / design system | `ServiceCard` price/duration mini-labels (`<dt>` in `.bg-[var(--khalsni-public-bg-secondary)]`) render `#98a2b3` on `#f6f9ff` = **2.44:1** contrast (need 4.5:1), 12px bold. axe: serious `color-contrast` ×10 on `/`, ×7 on `/services`, ×1 each on `/employee`/`/admin`. 0 critical. | ≥ 4.5:1 for the label text (WCAG 2.2 AA). | one under-weight secondary-label colour token, reused everywhere. | Darken the secondary label token (e.g. `#667085` → 4.6:1 on `#f6f9ff`) or bump weight/size — **one token change**. Do not patch per route. | `frontend/e2e/a11y.spec.js` (axe gate, fails on serious/critical) — now in CI. |
| **D-UX1** | MEDIUM | P2 | i18n / UX | `LanguageSwitcher` is rendered only in `Topbar.jsx` (dashboard). **The entire public site has no language switcher** — a public visitor cannot reach English. English *works* when forced (`localStorage['khalisni-language']='en'` → `dir=ltr`, English nav + bodies, 0 overflow on 7 routes). | A visible, accessible language toggle in `PublicLayout` (header + mobile menu). | switcher never added to the public shell during the redesign. | Render `LanguageSwitcher` in `PublicLayout` header + mobile drawer; optionally honour `?lang=` and persist. | `frontend/e2e/run-english.mjs` records `_public_language_switcher_count: 0`; add an assertion once fixed. |
| **D-UX2** | MEDIUM | P2 | UI layout | "Services by category" section on `/` renders category groups' cards **left-flush with large empty space** at ≥1024px (grid does not fill the row). Same in AR (mirrored) and EN. No horizontal overflow, but visually broken. | Cards fill the available row (responsive grid) or the section width matches the content. | section container full-width with a non-filling flex/grid of fixed-width cards. | Use a responsive `grid`/`auto-fill` for the per-category service rows; cap section width to content. | route matrix screenshots `public-home-{ar,en}-1440.png`; add a visual assertion. |
| **D-DEP1** | HIGH | P1 | Security / dependencies | `npm audit --omit=dev` (production deps) → **4 high-severity** advisories in `react-router` / `react-router-dom` `7.8.2` (DoS via path expansion; CSRF via PUT/PATCH/DELETE; open redirect via backslash in `<Link>`/`useNavigate`; XSS/constructor-injection in RSC paths). `npm audit fix` available. Backend `pip-audit`: **0** vulnerabilities. | 0 high/critical in production deps. | dependency not bumped since the advisories published. | `npm audit fix` or pin `react-router-dom` to a patched 7.x; re-run `npm audit --omit=dev` in CI (already added). App is a client-only SPA so the RSC/SSR advisories are lower-impact, but open-redirect + route-matching-DoS apply. | CI `frontend` job `npm audit --omit=dev --audit-level=high` (added in Gate 2). |
| **D-AZ1** | — (reviewed) | — | Authorization | Providers/employees can call `/api/admin/dashboard/` and `/api/admin/reports/*`. | — | `CanViewReportsDashboard` admits them. | **No fix needed.** `_scoped_orders_for_reports` returns only the caller's own orders and every view **strips revenue** for non-`platform_super_admin`. SRS BR7 satisfied. Cosmetic: `/api/admin/` prefix on provider-reachable routes (P3). | `config/tests_authz_matrix.py` (matrix row + note); deep review in `KHALSNI_AUTHORIZATION_RUNTIME_MATRIX.md`. |
| **C-VIS1** | — (conflict) | P2 (decision) | Visual direction | The 2026-08-05 homepage reference JPEG is **dark navy**; the 2026-07-27 written redesign spec + 2026-07-26 reference set + 2026-09-01 implementation reports all mandate **light**. Live site is light (conforms to the written chain). | One consistent reference set. | a single un-briefed mockup contradicts the documented chain. | Product owner confirms light-vs-dark **in writing**; reconcile the reference folder. Not a blocker — light is the defensible current authority. | `KHALSNI_VISUAL_CONFORMANCE_MATRIX.md` |

---

## GATE 2 — remediation status (updated 2026-09-05)

| ID | Status | Evidence |
|---|---|---|
| D1 | **FIXED / VERIFIED** | **App layer:** `config/urls.py` `public_media_serve` allow-lists only `service_categories/ services/ public_site/ organizations/branding/ manual/`; every other prefix (incl. `secure_orders/`, `payments/`) and `..` traversal → 404. Regression: `documents/tests_remediation.py::MediaRouteAuthorizationTests` (3 tests). **Edge/prod layer:** `frontend/nginx.conf` `location /media/` now `deny all` (403) for `^/media/(secure_orders\|payments)/` — the production path previously served those directly from the volume. Documents are fetched via the authenticated `/api/documents/<id>/download/`. Full backend suite green. |
| D2 | **FIXED / VERIFIED** | `RelatedServiceSerializer` no longer exposes raw `service_fee`/`government_fee`; it now returns the visibility-gated `pricing` + `delivery_time` payloads (also a net FE improvement). Regression: `services/tests_remediation.py::RelatedServicePriceLeakTests` (2 tests). |
| D3 | **FIXED / VERIFIED** | `config/settings.py`: `_get_bool_env("DJANGO_DEBUG", False)`; missing `DJANGO_SECRET_KEY` now raises `ImproperlyConfigured` unless `DEBUG`. `.env.example` (both) updated. Regression: `accounts/tests_remediation.py::DebugDefaultTests`. CI already passes `DJANGO_SECRET_KEY`. |
| D4 | **FIXED / VERIFIED** | Access token 8h→30m (env `JWT_ACCESS_TOKEN_MINUTES`), `ROTATE_REFRESH_TOKENS=True`. New `CustomUser.token_version` (migration `accounts/0007`), `accounts/authentication.VersionedJWTAuthentication` wired as the default DRF auth class, `CustomTokenObtainPairSerializer.get_token` stamps the claim, bumped on password reset (`accounts/password_reset.py`) and `POST /auth/logout/ {all_devices:true}`. Regression: `accounts/tests_remediation.py::TokenVersionRevocationTests` (3 tests) — proves an old access token is 401 after logout-all. |
| D5 | **FIXED / VERIFIED** | `documents/file_validation.py` magic-byte check for the 6 allowed types (no new dependency); called from `documents/serializers.DocumentUploadSerializer.validate_file` and the `orders/serializers` order-create doc loop. Regression: `documents/tests_remediation.py::UploadSignatureTests` (4 tests) — HTML-disguised-as-PDF rejected. |
| D6 | **DEFERRED_WITH_REASON** | Dropping the legacy `ServiceRequiredDocument.document_type`/`name_ar` columns and forcing `document_definition` non-null is a destructive migration that needs a staging-data backfill audit first (rule 10). No code change this gate. Consequence: order-create doc validation still keys on `document_type` strings. Requires: a staging DB dump to write and validate `normalize_required_documents`. |
| D7 | **FIXED / VERIFIED** | New partial unique constraint `unique_undeleted_required_document_definition_code` (`WHERE is_deleted=False`), migration `services/0010`. Regression: `services/tests_remediation.py::RequiredDocumentDefinitionCodeUniquenessTests` (2 tests). |
| D8 | **FIXED / VERIFIED** | `GET /api/notifications/unread-count/` + `POST /api/notifications/mark-all-read/` (`notifications/views.py` + `urls.py`), scoped via `get_notifications_for_user`, `read_at`/`sent_at` kept consistent with the `notification_read_state_consistent` CheckConstraint (also fixed a latent bug in the existing single mark-read view). FE: `notificationsApi` + `api/services.js` helpers + a working "mark all read" button in `NotificationPanel`. Regression: backend `notifications/tests_remediation.py` (3 tests), FE `NotificationPanel.test.jsx`. |
| D9 | **FIXED / VERIFIED (config)** | `.github/workflows/ci.yml` now also runs `npm run build`, `npm audit --omit=dev --audit-level=high`, `pip-audit --strict`, mobile `npm run typecheck`, and a Playwright `e2e` job (browsers installed, report uploaded). Not executed in this local session — will run on the next push/PR. |
| D10 | **PARTIAL — harness added, not executed** | `frontend/playwright.config.js` (boots a seeded Django + built frontend), `frontend/e2e/public.spec.js` (public routes, console-error gate, overflow gate at 3 viewports, discovery click-path, 404, RTL/LTR), `frontend/e2e/a11y.spec.js` (axe WCAG 2.2 AA gate), `frontend/e2e/README.md`, deps + `e2e` script in `package.json`, `vite.config.js` `test.include` scoped to `src/**` so vitest ignores `e2e/`. **Not run here** — no browser runtime available in this environment; authenticated J04–J20 fixtures + negative-AuthZ matrix still to be written. |
| D-DB1 | **DEFERRED_WITH_REASON** | Adding a default `SoftDeleteManager` that filters `is_deleted` changes the base queryset of 20+ models and every restore/trash path; high regression surface against the whole suite (rule 9). Not attempted this gate. Consequence: deleted-row exclusion stays per-view (reviewed catalog/public paths are correct). Requires: a dedicated pass with `all_objects` audited call-by-call. |
| D-DB2 | **FIXED / VERIFIED** | `ServiceRelation.source_service`/`target_service` and `ServiceProviderAssignment.service`/`provider` FKs `CASCADE`→`PROTECT`, migration `services/0010`. Regression: `services/tests_remediation.py::SoftDeleteFkProtectionTests` (2 tests). |
| D-DB3 | **PARTIAL** | CI `e2e` job runs against Postgres-less SQLite still for the unit job; `backend/db.sqlite3` already git-ignored. A dedicated Postgres CI service job is left for the infra pass. |
| D11 | **FIXED / VERIFIED** | `.gitignore` extended (`frontend/dist-acceptance/`, `frontend/test-results/`, `frontend/playwright-report/`, `frontend/.ruff_cache/`); `git rm --cached` applied to the 160+ tracked `dist-acceptance` + `test-results` files (kept on disk). Root artefact ignores were already present. |
| C1 | **RESOLVED (doc aligned)** | `docs/SRS.md` FR2 updated to state order creation is authenticated-customer/guest-account, explicitly superseding the older "public users" wording and citing audit conflict C1. |
| U7 | **DEFERRED_WITH_REASON** | Full CSS-logical-property migration across all components is a large cross-cutting change better done against a running browser with the new Playwright RTL check as the guard. The RTL/LTR gate now exists in `e2e/public.spec.js`. |
| U14 | **FIXED / VERIFIED** | `frontend/src/pages/public/NotFoundPage.jsx`; `AppRoutes.jsx` `*` route renders it (inside `PublicLayout` and as the global fallback) instead of `<Navigate to="/">`. Regression: `NotFoundPage.test.jsx`; e2e assertion in `public.spec.js`. |
| U15 | **DEFERRED (P3)** | Starter-cruft removal (`App.css`, `vite.svg`, `react.svg`, `frontend/README.md`) not done — cosmetic, no functional impact, batched with the P3 cleanup. |
| A11Y-1 | **PARTIAL — gate added, not executed** | `e2e/a11y.spec.js` fails the build on any serious/critical axe violation on 6 public routes. Not run in this environment; violations (if any) will surface on the first CI `e2e` run. |
| D-DB / U1–U6, U8–U13 | **DEFERRED_WITH_REASON** | The design-system-consistency / box-in-box / radius / gray-overuse UX reconciliation from the 2026-08-30 customer UI audit needs a running browser and a design decision on the unified token scale. No route-specific CSS hacks were added. |

### Not addressed this gate (full design-system + visual-reference conformance + full responsive + full a11y sweep across ALL ~57 routes)
Reason: this requires (a) a browser-capable runtime to render, screenshot, and diff every route against the reference images in `image/`/`imges/`, (b) a design decision to unify the public vs. authenticated token scale, and (c) authenticated E2E fixtures. None are available in this session. The Playwright + axe harness, the RTL/LTR check, the overflow check, and the 404 page are the concrete down-payments; the remaining visual/UX work is scoped in `KHALSNI_REMEDIATION_PLAN.md` §4 (U1–U13) and `KHALSNI_UI_UX_AUDIT.md`.

---

| ID | Severity | Priority | Area | Requirement | Current behavior | Expected behavior | Root cause | Fix | Test |
|---|---|---|---|---|---|---|---|---|---|
| D1 | MEDIUM | P1 | Security / object-level access | SRS NFR6 "Protected document download paths" | `config/urls.py` mounts `re_path(r"^media/(?P<path>.*)$", django.views.static.serve, {"document_root": MEDIA_ROOT})` with **no auth**. Uploaded order documents live at `MEDIA_ROOT/secure_orders/<order_number>/documents/<uuid>.<ext>` and are retrievable by anyone who has the URL, bypassing `can_user_download_document`. | Files under `secure_orders/**` are served only through an authenticated, permission-checked path (`DocumentDownloadAPIView`, signed token, or nginx `internal` + `X-Accel-Redirect`). | A catch-all media route was added "for all environments" as a convenience; it is not scoped to public catalog media. | Restrict the `serve` route to public prefixes (`service_categories/`, `services/`, public-site assets) or remove it; force object storage (private S3) in production; document the nginx `internal` pattern. | Test: unauthenticated `GET /media/secure_orders/<known-order>/documents/<uuid>.pdf` returns 403/404; catalog image `GET /media/services/images/x.png` still 200. Confidence: **HIGH** the route exists and is unauthenticated; **MEDIUM** on real-world exploitability (uuid filenames are unguessable; needs a URL leak). |
| D2 | MEDIUM | P1 | Security / info-disclosure; catalog | A1/CE5 + acceptance #9 "public APIs expose only the selected price fields" | `GET /api/services/<slug>/` (`AllowAny`) → `related_services[]` via `RelatedServiceSerializer` returns raw `government_fee` and `service_fee` for up to 4 same-category services, **ignoring** `show_government_fee_public` / `show_company_fee_public`. | Related-service fee fields respect the same per-field visibility flags, or are omitted (the card only needs name/slug/image/duration). | `RelatedServiceSerializer.Meta.fields` lists `service_fee`/`government_fee` directly instead of going through `_public_pricing_payload`. | Remove the two fee fields from `RelatedServiceSerializer`, or replace with a `pricing` `SerializerMethodField` that reuses `_public_pricing_payload`. | Test: create a related service with all `show_*_public=False`; assert `government_fee`/`service_fee` absent/null in `related_services`. Confidence: **HIGH** (read directly). |
| D3 | MEDIUM | P1 | Security / configuration | Django deployment hardening | `config/settings.py`: `DEBUG = _get_bool_env("DJANGO_DEBUG", True)`. A deploy omitting `DJANGO_DEBUG` runs with debug on → stack traces, and all `SECURE_*`/cookie-secure/HSTS/SSL-redirect settings (keyed off `not DEBUG`) disabled. | `DEBUG` defaults to `False`; local dev opts in with `DJANGO_DEBUG=True`. | Convenience default favouring local dev. | Flip the default to `False`; update `.env.example` and `docs/DEPLOYMENT.md`; optionally assert `not DEBUG` in a prod-only startup check. | Test: with env unset, `settings.DEBUG is False` and `SECURE_SSL_REDIRECT is True`. Confidence: **HIGH**. |
| D4 | MEDIUM | P1 | Security / session | NFR3 JWT auth; `docs/current_status_2026-06-16.md` open item | `ACCESS_TOKEN_LIFETIME=8h`, `REFRESH_TOKEN_LIFETIME=7d`, `ROTATE_REFRESH_TOKENS=False`. Logout blacklists the refresh token only; a stolen access token stays valid up to 8h with no revocation on logout / password reset / role change. | Short access lifetime (≤30–60 min); refresh rotation + blacklist enabled; access-token invalidation on logout & credential change (token-version or `jti` deny-list). | MVP default never revisited. | Set `ACCESS_TOKEN_LIFETIME=timedelta(minutes=30)`, `ROTATE_REFRESH_TOKENS=True`; add a `token_version` on `CustomUser` checked in a custom auth class, bumped on logout-all / password reset / role change. | Test: after logout, the old access token is rejected within the grace window; after password reset, all prior tokens fail. Confidence: **HIGH** (config read). |
| D5 | LOW | P2 | Security / file upload | SRS FR10 "validated for type and size" | `documents/serializers.py::validate_file` and `Document.clean()` validate extension + size + **client-declared** `content_type`. No server-side content/magic-byte inspection. | Sniff the actual file bytes; reject if sniffed type is not in the allowlist or contradicts the extension. | `python-magic`/libmagic not a dependency. | Add `python-magic` (or `filetype`); validate the 6 allowed types by signature; keep extension + size checks. | Test: upload `evil.pdf` whose bytes are a PE/HTML → rejected. Confidence: **HIGH** (validators read). |
| D6 | LOW | P2 | Data model / catalog | A1/CE1, CE10, C3 | `ServiceRequiredDocument.document_definition` FK is `null=True`; legacy free-text `document_type` + `name_ar`/`name_en` remain populated and are what order-create validation matches on. | The definition FK is the single identity; legacy free-text columns removed after backfill; order-create validation keyed on `document_definition_id`/`code`. | Back-compat migration deliberately kept both paths; the second half ("deprecate old fields") not done. | Backfill `document_definition` for all rows; make FK non-nullable; drop/retire the free-text columns; re-point `orders/serializers.py` validation to the definition. | Test: order-create rejects a file whose `document_definition_id` is not linked to the service; renaming a definition does not break validation. Confidence: **HIGH**. |
| D7 | LOW | P2 | Data model / constraint | A1/CE1 ("`code` … unique identifier") | `RequiredDocumentDefinition.code = SlugField(unique=False)`. Uniqueness comes only from a **partial** unique index (`WHERE NOT is_deleted AND is_active`) plus a race-prone `clean()` check. Two inactive/deleted defs can share a code. | `code` globally unique (or at least a full unique index), so identity is stable across active/inactive/deleted. | Partial index chosen to allow "reuse a code after deleting the old one". | Decide the policy: either a global unique index on `code`, or keep the partial index but generate a unique suffix on soft delete. Add a DB-level guard, not just `clean()`. | Test: concurrent create of two defs with the same `code` → one fails at the DB. Confidence: **HIGH**. |
| D-DB1 | MEDIUM-LOW | P2 | Data model / soft delete | A1/CE7, A2 §2 (`.active()/.deleted()/.with_deleted()`) | `core.SoftDeleteModel` has no manager/queryset. Deleted-row exclusion is per-view; reviewed catalog/public paths are correct but there is no central guarantee. | A `SoftDeleteManager` (default `is_deleted=False`) + `all_objects` for admin/audit; helper querysets as A2 specified. | Mixin shipped without a manager. | Add `SoftDeleteManager`/`SoftDeleteQuerySet`; set `objects` + `all_objects`; audit every `.objects` usage that must include deleted rows (admin trash, restore). | Test: `Model.objects.count()` excludes soft-deleted; `Model.all_objects.count()` includes them. Confidence: **HIGH**. |
| D-DB2 | LOW | P3 | Data model | consistency with soft-delete intent | `ServiceRelation` / `ServiceProviderAssignment` FKs to `Service` use `on_delete=CASCADE` while `Service` is soft-deleted (never row-deleted via the app). | FKs to soft-deletable parents use `PROTECT` or `SET_NULL` so an accidental hard delete cannot silently erase relations. | Modelled before the soft-delete standardisation. | Change to `PROTECT`; rely on soft delete for lifecycle. | Migration + test. Confidence: **HIGH** (read). |
| D8 | LOW | P3 | Feature gap / notifications | `docs/current_system_full_analysis_2026-06-16.md` open item | No `GET .../notifications/unread-count/` and no `POST .../notifications/mark-all-read/`. | Both endpoints exist; the notification bell shows an unread badge and supports "mark all read". | Never implemented. | Add both to `notifications/urls.py` + views + api-client + UI badge. | API tests + FE test. Confidence: **HIGH** (grep = no match). |
| D9 | LOW | P2 | DevOps / CI | task §11, §16 | `.github/workflows/ci.yml` runs backend `check`+migration-check+`test` and frontend `lint`+`test` only. No `npm run build`, no mobile `typecheck`, no dependency/vuln scan, no E2E/axe/visual. | CI also builds the frontend, type-checks mobile, runs `pip-audit` + `npm audit`, and (eventually) a Playwright smoke. | CI scoped to the fast core early on. | Extend `ci.yml`; add Dependabot. | CI passes with the new steps. Confidence: **HIGH**. |
| D10 | MEDIUM | P1 (process) | Test architecture | task §11, §22 | No committed browser/E2E, axe, type-check, formatter, or dependency-scan runner anywhere. Frontend unit tests assert against mock fixtures (`src/api/services.js` test-mode), so they under-test the FE/BE contract. J01–J20 journeys and the multi-role negative-authorization matrix have no reproducible executable proof. | A committed Playwright suite (role journeys + viewport matrix + axe), a real API-contract test layer, and CI wiring. | Never built; prior verification was manual/screenshot-based. | Stand up `@playwright/test` + `@axe-core/playwright`; add DRF `APITestCase` contract tests; wire into CI. | Suite runs green in CI and covers J01–J20 + negative AuthZ. Confidence: **HIGH** (absence confirmed). |
| D-DB3 | LOW | P3 | Repo hygiene / env | — | Local dev + the local test run use SQLite (`backend/db.sqlite3` present in tree); production Postgres path not exercised this gate. | `db.sqlite3` git-ignored; a Postgres-backed test job in CI. | Default DB config + committed dev DB. | Add `backend/db.sqlite3` to `.gitignore` (if tracked); add a Postgres service to the CI backend job. | CI backend job runs on Postgres. Confidence: **MEDIUM** (SQLite inferred from `which python` fallback + file presence). |
| D11 | LOW | P3 | Repo hygiene | — | Repo root holds ~40 loose artefacts (`*.pdf`, `*.docx`, `*.xlsx`, `*.jpeg/png`, `live.js`, `live-index-*.js`, `generate_manual.py`, a `~$…docx` Office lock file); `frontend/dist/` + `frontend/dist-acceptance/` build outputs are in the tree; `frontend/src/assets/{vite,react}.svg`, starter `App.css`, Vite-template `frontend/README.md`. | Specs/design assets under `docs/assets/`; build outputs git-ignored; starter cruft removed. | Accumulated over the project. | Reorganise; extend `.gitignore`. | `git status` clean after a build; no build output tracked. Confidence: **HIGH**. |
| C1 | — (conflict) | P1 (decision) | Requirements | SRS FR2 vs code | SRS: "Public users can create a service request." Code: `POST /api/orders/` requires `IsAuthenticated`+`IsCustomerRole`; public visitors are funnelled through registration / auto guest-account creation in the public-site flow. | A single documented rule. | Requirement evolved during the auth-hardening pass without an SRS update. | Product owner confirms "authenticated/guest customer only"; update `docs/SRS.md` FR2 and the traceability matrix. | n/a (doc change). |
| U7 | MEDIUM | P2 | UI / RTL | NFR1 Arabic-first RTL | Per `docs/ui/KHALSNI_CUSTOMER_UI_AUDIT.md` (2026-08-30): components force right-alignment and physical icon positions rather than CSS logical properties; RTL/LTR behaviour inconsistent. No automated RTL test exists. | Genuine bidi layout using logical properties; verified in both directions. | Incremental styling without a bidi discipline. | Migrate to logical properties (`ms-`/`me-`, `text-start`); add a Playwright RTL/LTR viewport check. | RTL snapshot tests for key routes. Confidence: **MEDIUM** (carried from prior audit; A6 claims partial fixes). |
| U14 | LOW | P2 | UI | UX | `*` route → silent `<Navigate to="/">`. A mistyped/expired deep link drops the user on the homepage with no explanation. | A real 404 page with navigation back. | Router catch-all set to redirect. | Add a `NotFoundPage`; route `*` to it. | FE route test. Confidence: **HIGH** (read `AppRoutes.jsx`). |
| U15 | LOW | P3 | UI / cleanliness | — | Aug-30 LOW: `App.css` starter CSS, `assets/vite.svg`+`react.svg`, Vite-template `frontend/README.md`, some hardcoded labels not in locale keys. | Removed / localised. | Template leftovers. | Delete; move stray labels into `src/locales/`. | lint + FE tests still green. Confidence: **HIGH**. |
| A11Y-1 | MEDIUM | P2 (→ P0 if a confirmed blocker) | Accessibility | task §16, WCAG 2.2 AA | No automated axe run, keyboard-flow test, contrast audit, SR-label check, or modal focus-trap verification. Baseline semantics present per A7. | WCAG 2.2 AA conformance evidenced by an axe run + keyboard walkthrough of every route class. | No a11y tooling in the repo. | Add `@axe-core/playwright`; run on all route classes; fix violations. | axe suite green. Confidence: **HIGH** (absence confirmed). |

---

## Carry-forward UX items (from `docs/ui/KHALSNI_CUSTOMER_UI_AUDIT.md` 2026-08-30, not re-verified this gate)

U1 box-in-box `ServiceCard`; U2 nested counters on `ServicesPage`; U3 price/duration repeated 3× on `ServiceDetailsPage`; U4 hardcoded category-image logic in `HomePage`; U5 low-opacity `CategoryCard` background image; U6 radius-scale inconsistency public vs customer dashboard; U8 gray/slate over-use; U9 uncentralised card heights/image ratios; U10 split search UX; U11 duplicated button styling; U12 customer dashboard visual divergence; U13 floating contact action overlap. All **P2 UX**. A6 (2026-09-01) claims several were addressed in the "final consolidation"; a runtime reconciliation pass is required to close them.

---

## Count (post-Gate-1R)

| Severity | Open items |
|---|---|
| BLOCKER | 0 |
| CRITICAL | 0 |
| HIGH | **1** — D-DEP1 (react-router prod advisories; `npm audit fix` available) |
| MEDIUM | **6** — D-A11Y-1, D-UX1, D-UX2 (new, runtime-evidenced) · D-DB1, U7 (deferred from Gate 2) · CE10/D6 (deferred) |
| LOW / P3 | D-DB3, U15, D-AZ1-cosmetic, U1–U6/U8–U13 UX reconciliation (12) |
| Conflicts (decision) | C-VIS1 (light vs dark reference) — C1 already resolved |
| Runtime-verified fixed this gate | D1, D2, D3, D4, D10, U14 |

### Legacy count table (pre-Gate-1R, retained)

| Severity | Count | Fixed & verified | Deferred / partial |
|---|---|---|---|
| BLOCKER | 0 | – | – |
| CRITICAL | 0 | – | – |
| HIGH | 0 | – | – |
| MEDIUM | 8 (D1, D2, D3, D4, D10, D-DB1, U7, A11Y-1) | 4 (D1, D2, D3, D4) | 4 (D10, D-DB1, U7, A11Y-1) |
| LOW | 9 (D5, D6, D7, D8, D9, D-DB2, D-DB3, U14, U15) | 6 (D5, D7, D8, D9, D-DB2, U14) | 3 (D6, D-DB3, U15) |
| Requirement conflict | 1 (C1) | 1 (doc aligned) | – |
| Carry-forward UX (P2, unverified) | 12 (U1–U6, U8–U13) | 0 | 12 |

**Fixed & verified this gate: 11** (D1, D2, D3, D4, D5, D7, D8, D-DB2, U14 + C1 doc + D9 CI config), each with a backend or frontend regression test.
**Deferred with reason: 6** (D6, D-DB1, D-DB3, U7, U15, + the U1–U13 UX reconciliation).
**Partial (harness added, execution needs a browser CI runner): 3** (D10 E2E, A11Y-1 axe, D9 e2e job).
