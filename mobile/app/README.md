This folder is reserved for future Expo Router migration.

The current implementation uses `App.tsx` with React Navigation to keep the role-based navigator explicit and aligned with the existing Khalsni web routing model.
